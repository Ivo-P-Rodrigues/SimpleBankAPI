#docker-compose -f "SimpleBank-docker-compose.yml" up --build

version: '3.7'
name: 'SimpleBank-demo'
networks:
  internal:
    driver: bridge
services:

  postgres:
    ports:
      - "5432:5432"
    image: postgres:11.1
    environment:
      POSTGRES_DB: bank-api-local
      POSTGRES_PASSWORD: postgres
      POSTGRES_USER: postgres
      
  # Kafka - zookeper
  zookeeper:
    image: confluentinc/cp-zookeeper:7.0.1
    container_name: zookeeper
    environment:
      ZOOKEEPER_CLIENT_PORT: 2181
      ZOOKEEPER_TICK_TIME: 2000
    networks:
      - internal
    ports:
      - "2181:2181"
      
  # Kafka - broker
  broker:
    image: confluentinc/cp-kafka:7.0.1
    container_name: broker
    ports:
      - "9092:9092"
    depends_on:
      - zookeeper
    environment:
      KAFKA_BROKER_ID: 1
      KAFKA_ZOOKEEPER_CONNECT: "zookeeper:2181"
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: PLAINTEXT:PLAINTEXT,PLAINTEXT_INTERNAL:PLAINTEXT
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://localhost:9092,PLAINTEXT_INTERNAL://broker:29092
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
      KAFKA_TRANSACTION_STATE_LOG_MIN_ISR: 1
      KAFKA_TRANSACTION_STATE_LOG_REPLICATION_FACTOR: 1
      KAFKA_CREATE_TOPICS: "bank-api-local:1:1"      
    networks:
      -  internal

#  simplebank:
#    build:
#      context: .
#      dockerfile: SimpleBank.AcctManage.API/Dockerfile
#    ports:
#        - "5000:443"
#    networks:
#        - internal
#    depends_on:
#      - broker
#    environment:
#      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT_INTERNAL://broker:29092


#    volumes:
#        - ../app:/app
#    links:
#        - postgres
