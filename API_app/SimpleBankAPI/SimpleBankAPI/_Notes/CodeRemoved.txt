
´Context
	    protected override void OnConfiguring(DbContextOptionsBuilder optionsBuilder)
        {
            if (!optionsBuilder.IsConfigured)
            {
                optionsBuilder.UseNpgsql("Server=localhost;Port=5432;Database=SimpleBankAPI;User Id=postgres; Password=t8C14al5;");
            }
        }



TransferResponse : IValidatableObject
        public IEnumerable<ValidationResult> Validate(ValidationContext validationContext)
        {
            if (FromAccountId == ToAccountId)
            {
                yield return new ValidationResult(
                    "FromAccountId cannot be equal to ToAccountId", 
                    new[] { nameof(FromAccountId), nameof(ToAccountId) }); 
            }
        }





Controllers
    using System.Security.Claims;
    User.FindFirst(ClaimTypes.NameIdentifier)?.Value


